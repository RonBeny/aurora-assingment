// a demo job for running scripts, can be added with requierment, tests and more 
pipeline {
    agent any
    stages {
       stage('Build') {
        steps {
            sh 'python main.py'
        }
      }
    }
}